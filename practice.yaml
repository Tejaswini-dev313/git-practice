- name: install nginx and run 
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  
  - name: enable services
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes